# Python
__pycache__/
*.pyc
*.pyo
venv/

# Environment & secrets
.env

# Data & databases
data/
logs/

# Third-party annual-report PDFs used as FinEx test fixtures.
# These are copyrighted documents belonging to JLR, Tesco, Unilever, Vodafone,
# Shell and HSBC — they must NOT be redistributed from a public repo. They were
# purged from git history on 2026-09-02. Keep them locally so the FinEx
# extraction tests can run; the tests skip cleanly when they are absent.
FinEx Data/
_finex_stage/

# Zip files
*.zip

# VS Code
.vscode/

# Secrets
credentials.json
token.json

# Secrets — never commit these
.env
token.json
credentials.json
data/chroma/
data/jarvis.db
*.icloud
venv/
venv.icloud.bak/

# macOS
.DS_Store

# Large media — exceeds GitHub's 100MB file limit
*.MOV
*.mov
*.mp4

# Scratch / working artifacts
_to_delete/
_sync2/
_*.png
*.diff
