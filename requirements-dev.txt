# Jarvis — development / test dependencies
# Install with:  pip install -r requirements-dev.txt
#
# Kept separate from requirements.txt so the runtime install stays lean.

-r requirements.txt

# ── Test suite (Phase 5) ───────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0

# ── Runtime dep added in Phase 2 ───────────────────────────────────────────
# JSON Schema validation for the common tool interface. Also the exact schema
# dialect MCP uses, so Phase 3 needs no translation layer.
jsonschema>=4.21.0
